% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{get_atlas_versions}
\alias{get_atlas_versions}
\title{Returns the atlas version changelog as a tibble}
\source{
\href{https://www.biorxiv.org/content/10.1101/2023.06.08.542671v3}{Mangiola et al.,2023}
}
\usage{
get_atlas_versions(cache = tempdir())
}
\arguments{
\item{cache}{Optional character scalar. A local directory used to
cache the downloaded parquet file. Defaults to a temporary directory to
separate from the main cache directory.}
}
\value{
A tibble with columns:
\describe{
\item{atlas_id}{Atlas version identifier, e.g. \code{"cellxgene_2024/0.1.0"}.
Matches the \code{atlas_id} column in \code{\link[=get_metadata]{get_metadata()}}.}
\item{census_version}{The CellxGene Census snapshot this atlas was built
from, e.g. \code{"01-07-2024"}.}
\item{change_type}{One of \code{"initial"}, \code{"patch"}, \code{"minor"}, or
\code{"major"}. See \code{ATLAS_VERSIONS.md} for the conventions standards.}
\item{description}{Summary text of what changed in this release.}
\item{modified_at}{Modification date as a character scalar (\code{"YYYY-MM-DD"}).
By default use \code{Sys.Date()}}
}
}
\description{
Downloads the \code{atlas_versions.parquet} registry from the cellNexus metadata
store, caches it locally, and returns it as an in-memory tibble. Each row
describes one atlas data release and its relationship to a CellxGene Census
snapshot.
}
\details{
The \code{atlas_id} column in this table corresponds directly to the \code{atlas_id}
column returned by \code{\link[=get_metadata]{get_metadata()}}, so you can join them to find which
Census snapshot any cell in your query came from.
}
\examples{
get_atlas_versions()
}
\references{
Mangiola, S., M. Milton, N. Ranathunga, C. S. N. Li-Wai-Suen,
A. Odainic, E. Yang, W. Hutchison et al. "A multi-organ map of the human
immune system across age, sex and ethnicity." bioRxiv (2023): 2023-06.
doi:10.1101/2023.06.08.542671.
}
\seealso{
\itemize{
\item CellxGene Census data releases (LTS):
\url{https://chanzuckerberg.github.io/cellxgene-census/cellxgene_census_docsite_data_release_info.html}
}
}
